import { getLocalBackupUiSnapshot, compareLocalVsCloud, getBackupCompareLabel } from '../../analytics/backup-summary.js'; import { renderCloudStatPair } from './cloud-ui-helpers.js'; import { renderRestoreDiffHtml } from './restore-diff.js'; import { compareBackupBranches } from '../../analytics/backup-branch-compare.js'; import { BackupVault } from '../../analytics/backup-vault.js'; import { getDeviceSettingsSemanticHash } from '../../analytics/backup-upload-runner.js'; import { esc, fmtDateTime } from './profile-ui-kit.js';
const DEVICE_LABEL_KEY = 'yandex:onboarding:device_label', sS = v => String(v == null ? '' : v).trim(), dL = d => sS(d?.label || d?.sourceDeviceLabel || d?.class || d?.sourceDeviceClass || 'Устройство'), dC = d => sS(d?.class || d?.sourceDeviceClass || d?.platform || d?.sourcePlatform || ''), dI = d => /iphone|ipad|ios/i.test(`${d?.cls || d?.class || d?.platform || ''}`) ? '📱' : (/android/i.test(`${d?.cls || d?.class || d?.platform || ''}`) ? '🤖' : '💻');
const colDevs = ({ backup: b, items: i = [], meta: m = null } = {}) => { const map = new Map(), add = d => { const k = sS(d?.deviceStableId || d?.sourceDeviceStableId || ''); if(k && !map.has(k)) map.set(k, { key: k, label: dL(d), cls: dC(d), platform: sS(d?.platform || d?.sourcePlatform || ''), lastSeenAt: Number(d?.lastSeenAt || d?.timestamp || 0) }); }; (Array.isArray(b?.devices)?b.devices:[]).forEach(add); [m,...(Array.isArray(i)?i:[])].filter(Boolean).forEach(add); return [...map.values()].sort((x,y)=>(y.lastSeenAt||0)-(x.lastSeenAt||0)); };
const resDSS = async ({ disk: d, token: t, deviceKey: k } = {}) => { const sk = sS(k); if(!sk||sk==='__new__') return 'новое устройство — не применяется'; if(sk==='__shared_only__') return 'только общий прогресс'; if(!d||!t) return 'проверка недоступна'; try { const idx=await d.getDeviceSettingsIndex?.(t).catch(()=>null), it=(idx?.items||[]).find(x=>sS(x.deviceStableId)===sk); if(!it) return 'не найдено в облаке'; if(!it.semanticHash) return 'hash недоступен'; const lD=await BackupVault.buildDeviceSettingsObject().catch(()=>null), lH=lD ? await getDeviceSettingsSemanticHash(lD).catch(()=>'') : ''; if(!lH) return 'локальный hash недоступен'; return lH===it.semanticHash?'совпадают':'отличаются'; }catch{return 'проверка не удалась';} };
const askNm = cD => new Promise(r => { const f=sS(cD?.label||localStorage.getItem(DEVICE_LABEL_KEY)||'Моё устройство'), p=window.Utils?.profileModals?.promptName; if(!p)return r(f); p({title:'Название нового устройства',value:f,btnText:'Продолжить',onSubmit:v=>r(sS(v)||f)}); });
export const openFreshLoginRestoreModal = ({ meta: m, items: i = [], backup: b = null, onRestore: oR, onNewDevice: oN, onLater: oL, currentDeviceInfo: cD = null, disk: d = null, token: t = null } = {}) => { const sf=Array.isArray(i)&&i.length?i:(m?[m]:[]), lS=getLocalBackupUiSnapshot({name:'Слушатель'}), cS=m||sf[0]||{}, cmp=compareLocalVsCloud(lS,cS), cL=getBackupCompareLabel(lS,cS), dvs=colDevs({backup:b,items:sf,meta:m}), defD=dvs.length?dvs[0].key:'__new__'; const bH = `<div class="fresh-restore-card"><div class="fresh-hero"><div class="fresh-hero-icon">☁️</div><div><div class="fresh-hero-title">Облачная копия найдена</div><div class="fresh-hero-sub">Выберите версию и устройство. Без подтверждения ничего не восстановится.</div></div></div>${cD?`<div class="fresh-current-device"><div class="fresh-cap">Вы сейчас на</div><div class="fresh-current-title">${esc(cD.osIcon)} ${esc(cD.os)} · ${esc(cD.browser)}</div><div class="fresh-muted">Текущее имя: ${esc(cD.label||'Моё устройство')}</div></div>`:''}${renderCloudStatPair({localSummary:lS,cloudSummary:cS})}<div class="fresh-compare ${cmp.state==='conflict'?'is-warn':''}"><b>Сравнение</b><span>${esc(cL)} · ${esc(cmp.state)}</span></div><details class="fresh-details"><summary>Что будет объединено</summary><div id="fresh-diff-box">${renderRestoreDiffHtml({backup:b,localSummary:lS,cloudSummary:cS,devices:dvs})}</div></details><div class="fresh-section-title">Версия backup</div><div class="fresh-scroll fresh-version-list" id="fresh-list">${sf.length?sf.map((it,idx)=>`<label class="fresh-version-card ${idx===0?'is-picked':''}"><input type="radio" name="fresh-ver" value="${idx}" ${idx===0?'checked':''}><span class="fresh-version-mark">${it.isLatest?'☁️':'🕘'}</span><span class="fresh-version-main"><b>${it.isLatest?'Последняя копия':'Архивная версия'}</b><small>${fmtDateTime(it.timestamp)}${it.sizeHuman?` · ${esc(it.sizeHuman)}`:''}${it.appVersion?` · v${esc(it.appVersion)}`:''}</small></span></label>`).join(''):'<div class="fav-empty">Облачных версий пока нет</div>'}</div><div class="fresh-section-title">Устройство из облака</div><div class="fresh-scroll fresh-device-list" id="fresh-dev-list">${dvs.map((dv,idx)=>`<label class="fresh-device-card ${idx===0?'is-picked':''}"><input type="radio" name="fresh-dev" value="${esc(dv.key)}" ${dv.key===defD?'checked':''}><span class="fresh-device-ic">${dI(dv)}</span><span class="fresh-device-main"><b>${esc(dv.label)}</b><small>${esc([dv.cls,dv.platform,dv.lastSeenAt?fmtDateTime(dv.lastSeenAt):''].filter(Boolean).join(' · '))}</small></span></label>`).join('')}<label class="fresh-device-card ${!dvs.length?'is-picked':''}"><input type="radio" name="fresh-dev" value="__new__" ${!dvs.length?'checked':''}><span class="fresh-device-ic">✨</span><span class="fresh-device-main"><b>Новое устройство</b><small>Создать новую запись и задать имя</small></span></label><label class="fresh-device-card"><input type="radio" name="fresh-dev" value="__shared_only__"><span class="fresh-device-ic">👤</span><span class="fresh-device-main"><b>Только общий прогресс</b><small>Без локальных настроек устройства</small></span></label></div><div class="fresh-actions"><button type="button" class="modal-action-btn online" data-fresh-act="restore">Восстановить</button><button type="button" class="modal-action-btn" data-fresh-act="later">Напомнить позже</button></div></div>`; const md=window.Modals?.open?.({title:'',maxWidth:390,strictClose:true,bodyHtml:bH}); if(!md)return; let bC=null, dSS=''; const rP=()=>{md.querySelectorAll('.fresh-version-card,.fresh-device-card').forEach(x=>x.classList.toggle('is-picked',!!x.querySelector('input')?.checked));}; md.addEventListener('change',()=>{rP();rDSS().catch(()=>{});}); rP(); const pV=()=>{const s=md.querySelector('input[name="fresh-ver"]:checked'),x=s?Number(s.value):0;return sS(sf[x]?.path||sf[0]?.path||'');}, pD=()=>sS(md.querySelector('input[name="fresh-dev"]:checked')?.value||defD), uDB=()=>{const bx=md.querySelector('#fresh-diff-box');if(bx)bx.innerHTML=renderRestoreDiffHtml({backup:b,localSummary:lS,cloudSummary:cS,devices:dvs,branch:bC,deviceSettingsState:dSS});}, rDSS=async()=>{dSS=await resDSS({disk:d,token:t,deviceKey:pD()});uDB();}; if(b)compareBackupBranches({backup:b}).then(br=>{bC=br;uDB()}).catch(()=>{}); rDSS().catch(()=>{}); md.addEventListener('click',async e=>{const btn=e.target.closest('[data-fresh-act]');if(!btn)return;const a=btn.dataset.freshAct;md.remove();try{if(a==='later')return oL?.();const pP=pV(),dv=pD();if(dv==='__shared_only__')return oR?.({pickedPath:pP,inheritDeviceKey:null,skipDeviceSettings:true});if(dv==='__new__'){const l=await askNm(cD);try{localStorage.setItem(DEVICE_LABEL_KEY,l)}catch{}return (oN||oR)?.({pickedPath:pP,inheritDeviceKey:null,asNewDevice:true,skipDeviceSettings:true});}return oR?.({pickedPath:pP,inheritDeviceKey:dv,skipDeviceSettings:false});}catch{}}); }; export default { openFreshLoginRestoreModal };
