import { bindProfileActions } from './actions.js'; import { bindProfileLiveBindings } from './live-bindings.js'; import { bindProfileAccount } from './account-bindings.js';
export const bindProfileTabControllers = ({ ctx, container, achView, profile, metaDB, onProfileChanged, reloadProfile } = {}) => { if(!container)return; bindProfileAccount({container,profile,metaDB,onProfileChanged}); bindProfileLiveBindings({ctx,getContainer:()=>document.getElementById('track-list'),achView,metaDB}); bindProfileActions({ctx,container,achView,metaDB,reloadProfile}); };
export default { bindProfileTabControllers };
